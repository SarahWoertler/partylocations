<!-- access @avails in js -->
<%= content_tag "div", id: "avails", data: {avails: @avails} do %>
<% end %>

<!-- build the page with the datepicker form -->

<h2> Select an available date for <%= @location.name %></h3>
<!-- <div class="container">
  <%= simple_form_for([:users, @avail], url: users_create_booking_path, method: :post) do |f| %>
  <%= f.input :date, collection: @avails_dates %>
  <%= f.submit :"Book Now" %>
  <% end %>
</div> -->

<div class="container">
    <div class="row">
        <div class='col-sm-6'>

          <%= form_for([:users, @avail], url: users_create_booking_path) do %>
            <div class="form-group">
              <div class='input-group date' id='datetimepicker1'>
                  <%= text_field_tag(:date, "", class: 'form-control') %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <br>
              <div class="input-group-btn">
                <button type="submit" class="btn btn-default" id="submit">Submit</button>
              </div>
            </div>
          <% end %>
        </div>
    </div>
</div>
